package fixtures.dynamicarrayforwardedparameterfinalswitchaliasmultiharmlesslocalsownedcomputedforcleanuprun

record Payload
    value: Int64

function forward_final_switch_alias_with_extra(flag: Bool, items: DynamicArray<Payload>) -> DynamicArray<Payload>
    switch flag
        true =>
            let alias: DynamicArray<Payload> = items
            var first = 1 as Int64
            var second = 2 as Int64
            alias
        false =>
            let alias: DynamicArray<Payload> = items
            var fallback = 3 as Int64
            alias

function main() -> UInt32
    var items: DynamicArray<Payload> = DynamicArray()
    var total = 0 as Int64
    items.push(Payload(13))
    items.push(Payload(17))
    for item in true ? forward_final_switch_alias_with_extra(true, items) : forward_final_switch_alias_with_extra(false, items)
        total = total + item.value
    0 as UInt32
