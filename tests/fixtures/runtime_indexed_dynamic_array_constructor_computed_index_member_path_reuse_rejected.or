package tests.fixtures.runtime_indexed_dynamic_array_constructor_computed_index_member_path_reuse_rejected

record Inner
    value: UInt32

record Outer
    item: Inner

function main() -> UInt32
    var items: DynamicArray<Inner> = DynamicArray()
    items.push(Inner(7 as UInt32))
    items.push(Inner(11 as UInt32))
    let index: UInt64 = 0 as UInt64
    var outer: Outer = Outer(items[index])
    items[index].value
