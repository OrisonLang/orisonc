package tests.fixtures.runtime_indexed_fixed_array_constructor_computed_index_move_run

record Inner
    value: UInt32

record Outer
    item: Inner

function main() -> UInt32
    var items: Array<Inner, 2> = [Inner(7 as UInt32), Inner(11 as UInt32)]
    let index: UInt64 = 0 as UInt64
    var outer: Outer = Outer(items[index])
    0 as UInt32
