package tests.genericfunctionternaryconstructorargumentmismatch

record Box<T>
    value: T

function value<T>(box: Box<T>) -> T
    box.value

function main() -> UInt32
    let flag: Bool = true
    value(flag ? Box(13 as UInt32) : Box(17 as UInt64)) - 13 as UInt32
