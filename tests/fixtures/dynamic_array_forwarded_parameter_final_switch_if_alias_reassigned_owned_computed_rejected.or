package fixtures.dynamicarrayforwardedparameterfinalswitchifaliasreassignedownedcomputedrejected

record Payload
    value: Int64

function forward_final_switch_if_alias_reassigned(selector: Bool, flag: Bool, items: DynamicArray<Payload>) -> DynamicArray<Payload>
    switch selector
        true =>
            var alias: DynamicArray<Payload> = items
            alias = items
            alias
        false =>
            if flag
                var alias: DynamicArray<Payload> = items
                alias
            else
                var alias: DynamicArray<Payload> = items
                alias

function main() -> UInt32
    var items: DynamicArray<Payload> = DynamicArray()
    var total = 0 as Int64
    items.push(Payload(13))
    items.push(Payload(17))
    for item in true ? forward_final_switch_if_alias_reassigned(true, true, items) : forward_final_switch_if_alias_reassigned(false, false, items)
        total = total + item.value
    0 as UInt32
