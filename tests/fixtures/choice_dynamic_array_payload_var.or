package tests.fixtures.choice_dynamic_array_payload_var

choice Buffered
    Ready(values: DynamicArray<UInt32>)
    Empty

function use_buffer() -> UInt32
    var buffer: Buffered = Empty
    0 as UInt32
