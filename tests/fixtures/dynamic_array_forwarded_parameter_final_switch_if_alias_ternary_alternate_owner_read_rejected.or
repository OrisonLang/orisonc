package fixtures.dynamicarrayforwardedparameterfinalswitchifaliasternaryalternateownerreadrejected

record Payload
    value: Int64

function forward_final_switch_if_alias_with_owner_in_ternary_alternate(selector: Bool, flag: Bool, items: DynamicArray<Payload>) -> DynamicArray<Payload>
    switch selector
        true =>
            let alias: DynamicArray<Payload> = items
            var marker = flag ? 1 as Int64 : items[0].value
            alias
        false =>
            if flag
                let alias: DynamicArray<Payload> = items
                alias
            else
                var alias: DynamicArray<Payload> = items
                alias

function main() -> UInt32
    var items: DynamicArray<Payload> = DynamicArray()
    var total = 0 as Int64
    items.push(Payload(13))
    items.push(Payload(17))
    for item in true ? forward_final_switch_if_alias_with_owner_in_ternary_alternate(true, true, items) : forward_final_switch_if_alias_with_owner_in_ternary_alternate(false, false, items)
        total = total + item.value
    0 as UInt32
