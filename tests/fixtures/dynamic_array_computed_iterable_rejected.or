package fixtures.dynamicarray.computediterable

function sum_words(flag: Bool, left: DynamicArray<UInt32>, right: DynamicArray<UInt32>) -> UInt32
    var total = 0 as UInt32
    for word in flag ? left : right
        total = total + word
    total
