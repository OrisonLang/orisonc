package fixtures.dynamicarray.computedlocalsameowneriterable

function sum_words(flag: Bool) -> UInt32
    let items: DynamicArray<UInt32> = DynamicArray()
    var total = 0 as UInt32
    for word in flag ? items : items
        total = total + word
    total
