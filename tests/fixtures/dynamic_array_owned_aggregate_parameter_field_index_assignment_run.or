package fixtures.dynamicarrayownedaggregateparameterfieldindexassignmentrun

record Payload
    public value: Int64

record Holder
    public items: DynamicArray<Payload>

function replace(holder: Holder) -> UInt32
    var current: Holder = holder
    current.items[0] = Payload(8)
    0 as UInt32

function main() -> UInt32
    var items: DynamicArray<Payload> = DynamicArray()
    items.push(Payload(7))
    replace(Holder(items))
