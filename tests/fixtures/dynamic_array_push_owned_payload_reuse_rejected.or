package fixtures.dynamicarraypushownedpayloadreuse

record Payload
    public value: Int64

function main() -> Int64
    var items: DynamicArray<Payload> = DynamicArray()
    let payload = Payload(7)
    items.push(payload)
    payload.value
