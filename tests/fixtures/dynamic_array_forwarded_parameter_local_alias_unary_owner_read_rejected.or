package fixtures.dynamicarrayforwardedparameterlocalaliasunaryownerreadrejected

record Payload
    value: Int64

function forward_alias_with_owner_in_unary_local(items: DynamicArray<Payload>) -> DynamicArray<Payload>
    var alias: DynamicArray<Payload> = items
    var marker = -items[0].value
    alias

function main() -> UInt32
    var items: DynamicArray<Payload> = DynamicArray()
    var total = 0 as Int64
    items.push(Payload(13))
    items.push(Payload(17))
    for item in true ? forward_alias_with_owner_in_unary_local(items) : forward_alias_with_owner_in_unary_local(items)
        total = total + item.value
    0 as UInt32
