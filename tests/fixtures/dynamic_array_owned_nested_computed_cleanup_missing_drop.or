package fixtures.dynamicarrayownednestedcomputedcleanupmissingdrop

record Payload
    public value: UInt32

function main() -> UInt32
    var items: DynamicArray<Payload> = DynamicArray()
    var total = 0 as UInt32
    for item in true ? items : false ? items : items
        total = total + item.value
    0 as UInt32
