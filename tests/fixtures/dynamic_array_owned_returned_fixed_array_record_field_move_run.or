package tests.fixtures.dynamic_array_owned_returned_fixed_array_record_field_move_run

record Payload
    value: UInt32

record Inner
    values: DynamicArray<Payload>
    spare: DynamicArray<Payload>

record Outer
    items: Array<Inner, 2>

function make_values(seed: UInt32) -> DynamicArray<Payload>
    var values: DynamicArray<Payload> = DynamicArray()
    values.push(Payload(seed))
    values.push(Payload(seed + 1 as UInt32))
    values

function make_inner(seed: UInt32) -> Inner
    Inner(make_values(seed), make_values(seed + 10 as UInt32))

function make_outer(seed: UInt32) -> Outer
    var items: Array<Inner, 2> = [make_inner(seed), make_inner(seed + 20 as UInt32)]
    Outer(items)

function main() -> UInt32
    var outer: Outer = make_outer(1 as UInt32)
    outer.items[0].values = make_values(50 as UInt32)
    0 as UInt32
