package fixtures.dynamicarrayforwardedparameterfinalswitchaliasunaryownerreadrejected

record Payload
    value: Int64

function forward_final_switch_alias_with_owner_in_unary_local(flag: Bool, items: DynamicArray<Payload>) -> DynamicArray<Payload>
    switch flag
        true =>
            let alias: DynamicArray<Payload> = items
            var marker = -items[0].value
            alias
        false =>
            let alias: DynamicArray<Payload> = items
            alias

function main() -> UInt32
    var items: DynamicArray<Payload> = DynamicArray()
    var total = 0 as Int64
    items.push(Payload(13))
    items.push(Payload(17))
    for item in true ? forward_final_switch_alias_with_owner_in_unary_local(true, items) : forward_final_switch_alias_with_owner_in_unary_local(false, items)
        total = total + item.value
    0 as UInt32
