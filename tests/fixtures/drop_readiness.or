package demo.dropreadiness

record Payload
    public value: Int64

implements Transferable for Payload
    function placeholder(this: shared This) -> Unit
        return

function launch(value: Int64) -> Int64
    let payload: Payload = Payload(value)
    let worker = thread
        payload.value

    worker.join()
