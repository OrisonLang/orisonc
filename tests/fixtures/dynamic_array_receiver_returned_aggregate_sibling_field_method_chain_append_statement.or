package fixtures.dynamicarray.receiverreturnedaggregatesiblingfieldmethodchainappendstatement

record Nested
    value: UInt32

record Payload
    child: Nested

record PairBox
    left: DynamicArray<Payload>
    right: DynamicArray<Payload>

extend DynamicArray<T>
    function forward(this: exclusive This) -> DynamicArray<T>
        this

    function append_value(this: exclusive This, value: T) -> Unit
        this.push(value)

function make_values(seed: UInt32) -> DynamicArray<Payload>
    var values: DynamicArray<Payload> = DynamicArray()
    values.push(Payload(Nested(seed)))
    values

function make_pair() -> PairBox
    PairBox(make_values(7 as UInt32), make_values(11 as UInt32))

function main() -> UInt32
    make_pair().left.forward().append_value(Payload(Nested(13 as UInt32)))
    0 as UInt32
