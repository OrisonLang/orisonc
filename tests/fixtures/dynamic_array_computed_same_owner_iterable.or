package fixtures.dynamicarray.computedsameowneriterable

function sum_words(flag: Bool, items: DynamicArray<UInt32>) -> UInt32
    var total = 0 as UInt32
    for word in flag ? items : items
        total = total + word
    total
