package fixtures.dynamicarray.receiverstaticindexedaggregatefieldmethodchainappendstatement

record Nested
    value: UInt32

record Payload
    child: Nested

record Bucket
    values: DynamicArray<Payload>

record Holder
    buckets: Array<Bucket, 2>

extend DynamicArray<T>
    function forward(this: exclusive This) -> DynamicArray<T>
        this

    function append_value(this: exclusive This, value: T) -> Unit
        this.push(value)

function make_values(seed: UInt32) -> DynamicArray<Payload>
    var values: DynamicArray<Payload> = DynamicArray()
    values.push(Payload(Nested(seed)))
    values

function make_holder() -> Holder
    Holder([Bucket(make_values(7 as UInt32)), Bucket(make_values(11 as UInt32))])

function main() -> UInt32
    make_holder().buckets[0].values.forward().append_value(Payload(Nested(13 as UInt32)))
    0 as UInt32
