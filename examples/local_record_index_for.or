package examples.collections

public record Grid
    public rows: Array<Array<UInt32, 3>, 2>

function main() -> UInt32
    var grid = Grid([[1, 2, 3], [4, 5, 6]])
    var total = 0 as UInt32
    for item in grid.rows[0]
        total = total + item
    0 as UInt32
