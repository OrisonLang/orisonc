package examples.aggregates

record Registers
    status: UInt32
    control: UInt32

function selected_status() -> UInt32
    let regs = Registers(7 as UInt32, 1 as UInt32)
    regs.status

function main() -> UInt32
    let value = selected_status()
    0 as UInt32
