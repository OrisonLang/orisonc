package examples.dynamicarrayownedreplacement

record Payload
    public value: Int64

function main() -> UInt32
    var items: DynamicArray<Payload> = DynamicArray()
    items.push(Payload(7))
    items[0] = Payload(8)
    0 as UInt32
